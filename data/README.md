# Data

`latest.min.json` is a lightweight public example used by the static page/demo.

The server keeps the fuller runtime files outside GitHub:

- `latest.json`
- `archive/YYYY-MM-DD.json.gz`
- raw parser outputs
- logs
